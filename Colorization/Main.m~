%this function returns a color guess and its corresponding shade
function [] = Main(colorType, shade)
    [x,~] = size(colorType);
    
    for i=1:x
        disp(i);
        disp('color type:');
        disp(colorType(i,:));
        disp('shade:');
        disp(shade(i,:));
    end
end