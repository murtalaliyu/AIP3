%THIS FUNCTION IS NOT IN USE AT THE MOMENT

%this function takes in an mx9 grey input and 
%returns a prediction of the true color of the center
function [color] = input2color(input)
    %have to predict color of center and return it
    [x,y] = size(input);
    color = zeros(x,y);
    %strategy for now
    for i=1:x
        
    end
end
